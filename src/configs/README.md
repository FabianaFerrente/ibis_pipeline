# Configuration Files

Contains YAML files defining paths, processing flags, calibration constants, and logging preferences.

## Files

- `pipeline_config.yaml` – Paths to data directories and pipeline options
- `logging_config.yaml` – Controls verbosity and format of logs

## Notes

These files should be editable by users without touching the codebase.
